# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
dist/
build/

# Virtual environment
venv/
.venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Dataset splits (regenerated by split_dataset.py from images/ + labels/)
dataset/images/

# Keep labels -- hand-edited, not fully regenerable
# Keep dataset/labels -- cheap, saves a conversion step
# Keep dataset/data.yaml -- class config

# Trained models directory (training artifacts, curves, etc.)
models/
# But keep the best weights (the deliverable)
!models/banana_obb/
!models/banana_obb/weights/
!models/banana_obb/weights/best.pt

# Pytest
.pytest_cache/

# Ultralytics runs (regenerated by evaluate.py)
runs/

# Downloaded model weights at repo root (large, auto-download on first use)
/*.pt

# LabelMe JSONs copied next to images for review (canonical copy is in labels/)
images/**/*.json

# Agentic files (per project standard)
CLAUDE.md
MEMORY.md
ANTIPATTERNS.md
